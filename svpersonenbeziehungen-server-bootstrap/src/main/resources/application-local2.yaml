#Bei Betrieb in local2 ist unser lokaler Serverport nicht 9999 wie das die Plattform erwarten würde. Darum wird diese hier überschrieben.
spring:
  boot:
    admin:
      client:
        instance:
          service-url: http://localhost:8096/at.gv.justiz.local2.svpersonenbeziehungen/

  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: EVMPBZ
    password: start
    testOnBorrow: true
    validationQuery: select 1 from dual

justiz3:
  server:
    id: svpersonenbeziehungen-local2
